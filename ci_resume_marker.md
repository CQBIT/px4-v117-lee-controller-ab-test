# CI resume marker

This branch update was created outside the repository `GITHUB_TOKEN` repair workflow to resume the PX4 v1.17 Lee-vs-body-rate SITL PR after a bot-authored commit produced an approval-required pull-request workflow run.

The current visualization-complete validation extends the existing real-SITL experiment with static trajectory/error/actuator plots, animated GIF trajectories, and persistence of validated lightweight results while keeping ULogs and PX4/Gazebo logs in Actions artifacts only.
